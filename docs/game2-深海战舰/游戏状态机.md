# 红魔特种兵2-深海战舰（game2）— 游戏状态机

> 已据 CFR 恢复方法补全。
> 主状态变量为 `a.b`（字段定义 `a.java:30`），由主线程 `paint()`（`a.java:126`）的 `switch(this.b)`（`a.java:132`）驱动。
> 另有场景内子状态机 `j.w`（在 `b==10`/`b==22` 时生效），见末节。
> 全部 `文件:行号` 引用已按现行 CFR 归档 `reverse/game2/2-decompiled-cfr/tjge/`（CFR 0.152）重新锚定。早期版本行号原按 jadx 编写，而 jadx 产物已删除、现行归档为 CFR-only（弃用 jadx 的历史对比见 [`reverse/反编译技术选型.md`](../../reverse/反编译技术选型.md)）；若再次出现偏移，请以 `2-decompiled-cfr/` 现行文件为准重核。

---

## 1. 主状态变量 `a.b` 取值与含义

| `b` 值 | 名称 | 含义 | paint 行 |
|---|---|---|---|
| **1** | LOADING_PROGRESS | 标题画面 + 资源进度条加载（`j.a(this,j)` 逐步预载） | 182-207 |
| **2** | LEVEL_INTRO_LOADING | 关卡加载中（黑屏 + "载入中"，调 `a(this.k)` 步进） | 314-326 |
| **3** | SPLASH | 启动 Logo 序列（厂商/发行/标题，多帧切换） | 133-181 |
| **4** | MAIN_MENU | 主菜单（返回游戏/新游戏/继续/声音/帮助/关于/退出） | 208-295 |
| **6** | HELP | 帮助页（折行文本 + 上下翻页） | 514-534 |
| **10** | IN_GAME | **游戏进行中**（`z.b()` 更新 + `z.a(g)` 渲染） | 372-376 |
| **16** | LEVEL_CLEAR | 关卡完成结算页（击杀/继续数/用时 + "完成"动画） | 446-497 |
| **18** | LEVEL_FAILED | 关卡失败菜单（重玩/返回 二选一 + 结算） | 377-445 |
| **19** | ABOUT | 关于页（版权/发行信息，单页折行文本） | 535-545 |
| **20** | CUTSCENE_INTRO | 新游戏开场过场：黑屏 + 两块作战区域框渐入 + 逐字打字剧情对话；出口 `b=2` 进首关加载 | 327-330（调 `a(graphics)`，`a.java:583`） |
| **21** | GAME_COMPLETE | 通关画面（"游戏结束"渐显） | 498-513 |
| **22** | MISSION_BRIEF | 关卡任务简报（任务目标 + 进度勾选 + 剧情对话） | 331-371 |
| **100** | NO_SAVE | "没有存档记录!!"提示（短暂后回主菜单） | 296-313 |

> 注意：`b` 的取值不连续（缺 0/5/7/8/9/11-15/17）；这些缺号是 tjge 引擎在游戏1 用过、游戏2 未复用的历史值。

---

## 2. 状态转移表（条件 + 触发点）

| 从 | 到 | 触发条件 | 触发点 `文件:行号` |
|---|---|---|---|
| (构造) | **3** SPLASH | `a(GameMIDlet)` 构造时 `this.b=3` | a.java:80 |
| **3** | **1** | Logo 序列播到 `j==65`（最后一帧） | a.java:172-180 |
| **1** | **4** MAIN_MENU | 进度条满：`j.a(this,this.j)` 返回 true（即 `j>=23`，进度按 `j*168/23`） | a.java:199-206 |
| **4** | **10** | 选"返回游戏"(`l==0`) + 确认键(`c==16`) | a.java:236-240 |
| **4** | **20** | 选"新游戏"(`l==1`) + 确认：清存档`b(2)`、`k=0`、`g.f[1]=6` | a.java:242-249 |
| **4** | **100** | 选"继续"(`l==2`) 但 `k[3]==0`（无进度） | a.java:251-256 |
| **4** | **20 或 2** | 选"继续" 且 `k[3]!=0`：读存档 `k=k[3]`、继续数+1、`g.f[1]=k[4]`；`k==0`→20 否则→2 | a.java:257-267 |
| **4** | (切声音) | 选"声音"(`l==3`)：`k[2]^=1`，关时 `c()` 停音；停留 4 | a.java:269-273 |
| **4** | **6** HELP | 选"帮助"(`l==4`)：载 `string.bin[7]`→`n` | a.java:275-280 |
| **4** | **19** ABOUT | 选"关于"(`l==5`)：载 `string.bin[8]`→`n` | a.java:282-286 |
| **4** | (退出) | 选"退出"(`l==6`)：`x.destroyApp(true)` | a.java:288-289 |
| **4** | **4** | 上键(`c==4`)/下键(`c==8`)：移动光标 `l`（环绕 `h..6`） | a.java:226-235 |
| **6** | **4** | 右软键(`c==16384`)：返回菜单，清 `n` | a.java:528-532 |
| **6** | **6** | 下键(`c==32768`)：翻页 `j+=9`（>26 回 0） | a.java:520-526 |
| **2** | **10** | 加载完成 `M==true`：`b=10`，记开始时间 `f` | a.java:321-324 |
| **10** | **22** | 键 22/-22（`b!=4` 时映射 32768，键码表 a.java:858-860）且 `z.w==0`：`e=0;b=22` 进任务简报（键触发进 22 的路径；另有经 `L.a()`/a.java:576 的脚本路径，见 §4.1） | a.java:876-878 注：keyPressed |
| **10 或 22** | **4** | 键 21/-21（映射 16384）：一律退回菜单（`b=4;h=0;l=0;c=0`） | a.java:868-875 |
| **10** | **16/18** | 本关结束由 `GameCanvas.a(boolean bl)`（CFR 基准 `a.a(boolean)`，友好方法名 `showResult`）切换：胜(bl=true)→16，败(bl=false)→18；j 内经 `L.a(true)`/`L.a(false)` 调用（如 j.java:289/326/395、败例 j.java:653） | a.java:552-565 |
| **10** | **4** | `hideNotify()`（失焦） | a.java:567-574 |
| **16** | **2 或 21** | "完成"动画结束(`G==34 && H++==4`)：存档`b(1)`；非末关 `k++`→2，末关(`k==6`)→21 | a.java:475-487 |
| **18** | **2** | 选"重玩"(`l==0`)+确认：继续数+1，`b=2` | a.java:418-424 |
| **18** | **4** | 选"返回"(`l==1`)+确认：`b=4` | a.java:425-429 |
| **19** | **4** | 右软键(`c==16384`)：返回菜单，清 `n` | a.java:540-543 |
| **20** | **2** | 过场最后一段对话播完并退场(`e` 从 4 递减到 `D<=0`)：`b=2` 进首关加载 | a.java:629-632 |
| **20** | **2** | 过场期间按下键(`c==32768`)：直接跳过过场，`b=2` | a.java:592-594 |
| **20** | **20** | 文本翻页/逐字推进（确认键 `c==16` 或左软键 `c==16384` 时强制推进）：见 §2.1 `a.F[]` 逻辑 | a.java:647-664 |
| **21** | **4** | "游戏结束"显示 `j>30`：`b=4;h=1;l=1` | a.java:506-509 |
| **22** | **10** | 黑幕收起完成(`e<0`)：`b=10`（在 `case 22` paint 内驱动） | a.java:342-344 |
| **100** | **4** | 计时 `j>15`：回菜单 | a.java:308-311 |

> 重要细节：
> - **`b=20` 是新游戏开场剧情过场，并非加载进度**——据 CFR 恢复的 `a(Graphics)`（a.java:583）确认它有独立的 `switch(this.e)` 子状态机（绘制区域框 + 逐字对话），**与 `b=1`/`b=2` 加载/进度条逻辑无关**（此前 jadx 推测有误，已订正）。详见 §2.1。
> - 过场出口是 **`b=2`（首关加载）而非 `b=4`**（a.java:631-632；按下键跳过时见 a.java:592-594）。新游戏由菜单进入 `b=20` 时已先设 `k=0;g.f[1]=6`（a.java:242-249），故过场结束后 `b=2` 加载的是第 0 关。
> - **进度条**（仅 `b=1` 标题加载用）按 `(this.j*168)/23` 绘制（a.java:194-198），说明加载分 **23 步**（对应 `j.O` 表 23 个类型）。
> - 关卡 `k` 范围 **0~6**（7 关）；末关 `k==6` 通关进 21。

---

## 2.1 `b==20` 开场过场内部状态机（`a(Graphics)`，a.java:583-668）

> 据 CFR 恢复。此前 jadx 反编译失败，文档曾误以为它复用加载进度逻辑——实为一段独立的剧情过场。
> 本节行号基于权威 CFR 源 `reverse/game2/2-decompiled-cfr/tjge/`。
> `paint` 的 `case 20`（a.java:327-330）每帧调 `this.a(graphics)`。该方法用字段 `e` 作为子状态游标，配合静态文本游标数组 `a.F[4]`（定义 `a.java:1005`，`static{}` 初值见 `a.java:1002-1005`）。

每帧先黑屏填充（a.java:589-591），并检查"跳过"按键：

- **下键 `c==32768`**（按 `b!=4` 的键映射，`a.java:858-860`）：立即 `b=2;return`（a.java:592-594），整段过场可跳过。

随后 `switch(this.e)`（a.java:598-633）：

| `e` | 动作 | 行 |
|---|---|---|
| **0** | 初始化 `E=0;C=0`；`e++` | 599-604 |
| **1** | `E=1`，区域框 1 渐入计数 `C++`，到 20 封顶后 `D=0`、`e++` | 605-612 |
| **2** | `E=2`，区域框 2 渐入计数 `D++`，到 20 封顶后：**载入对话文本** `GameMIDlet.a(0,"/res/string.bin")`（取 string.bin 第 0 条到 `GameMIDlet.l`）、`c=0`、`e++`，并复位文本游标 `F={0,0,2,60}` | 613-625 |
| **3** | 对话推进主态（什么都不在 switch 里做，逻辑在 switch 之后） | 626-628 |
| **4** | 退场：区域框 2 收回计数 `--D`，`D<=0` 时 **`b=2`** 进首关加载 | 629-632 |

`E` 控制已显示的元素层级（a.java:634-642）：`E>0` 画上方区域框（随 `C` 展开），`E>1` 画两块作战区域框（随 `D` 展开）+ 两个图标。

**`e==3` 的逐字对话推进**（a.java:643-664，仅当 `e==3` 时进入 `block17` 外的对话块）：

- `bl2 = c(F[0])`：`c(int)`（a.java:943-957）是段落分页器，按字符 `\r`(13) 切分 `GameMIDlet.l`，把第 `F[0]` 段取到 `GameMIDlet.m`；**返回 true 表示这是最后一段**。
- `bl = a(graphics, F[1], F[2], A[F[0]%2], B[F[0]%2], 90, 19)`：逐字渲染器（a.java:959-978），把 `GameMIDlet.m` 从行偏移 `F[1]` 起、行高 `F[2]` 逐行画出；`A/B`（`{17,46}`/`{29,106}`，a.java:1003-1004）按段落奇偶给出左侧绘制坐标（交替说话人位置）；**返回 true 表示本段已整段显示完**。
- 推进时序由 `F[3]` 倒计时与按键共同控制：
  - `bl3 = (c==16384 || c==16)`：**左软键或确认键**则强制推进（a.java:649）。
  - 否则 `F[3]--`，仍 `>=0` 时本帧不推进（停顿展示，a.java:651-653）。
  - 触发推进后（a.java:655-664）：若 `bl2`（最后一段）→ `e++`（进 `e==4` 退场）；否则若 `bl`（本段已满）→ 下一段 `F[0]++`、回到行首 `F[1]=0`、重置停顿 `F[3]=60`；否则（本段未满）→ 继续下卷 `F[1]+=F[2]`、`F[3]=60`。
- 末尾 `c=0`（a.java:666）清空按键，避免重复触发。

> ⚠️待核实：`A/B` 仅 2 项而段落用 `F[0]%2` 取址，说明开场对话固定为"双方轮流"的两个说话人位置；具体台词文本在 `string.bin[0]` 内、未在本文档展开。

---

## 3. 关卡加载子状态（`a.a(int)`，a.java:749-772）

`b==2`（LEVEL_INTRO_LOADING）每帧调 `a(this.k)`，内部用字段 `j` 作三步子状态：

| `j` | 动作 | 行 |
|---|---|---|
| 0 | 若已有场景且关号变了：卸载旧场景 `z.d()`；`j=1` | 752-758 |
| 1 | 若新关号≠当前 `j.z`：`z=j.b(this,n)`（n 为传入关号）创建新场景；`j=2` | 760-765 |
| 2 | `b()` 对焦相机；置 `M=true`（加载完成） | 767-771 |

`M=true` 后回到 `paint` 的 case 2，转 `b=10`（a.java:321-324）。

---

## 4. 场景子状态机 `j.w`（在 `b==10`/`b==22` 内生效）

`j.w`（字段定义 `j.java:52`）由 `j.b()`（`j.java:128-203`）的 `switch(this.w)` 驱动，控制关卡内的演出/战斗节奏。切换入口 `j.a(int)`（`j.java:773-793`，注意 `j.java:672` 是另一个方法 `f()`，勿混用）。

| `w` | 名称 | 含义 | `j.java` 行 |
|---|---|---|---|
| **0** | NORMAL | 常规游玩（相机跟随玩家，`a(true)`） | 133-136 |
| **1** | CUTSCENE | 剧情演出（运镜+脚本生成，调 `e()`） | 167-171 |
| **2** | TRANSITION_OUT | 屏幕切出（黑幕展开 `y+=3`，到 15 跳玩家位） | 176-184 |
| **3** | TRANSITION_IN | 屏幕切入（黑幕收起 `y-=3`，回 `x`） | 185-188 |
| **4** | MISSION_DIALOG | 任务/对话框（底部文字框逐字推进，调 `f()`，`j.java:672`；渲染 `c(graphics)`，`j.java:754`） | 172-175 |
| **5** | BATTLE_WAVE | 触发战斗波（守点刷敌，触发器类型1） | 137-151 |
| **6** | BOSS_SCRIPT | Boss 关剧本（`e()` 内各 `i[0]` 分支） | 152-166 |

转移要点：
- `j.a(int n)`（j.java:773-793）：保存返回态 `x=w`，设 `w=n`，复位 `M=N=0`；其中进入 `w=4` 会复位对话游标 `a.F={j[0],0,1,60}`、清按键（j.java:779-787）。
- 触发器执行 `j.a(int n, boolean bl)`（j.java:1018）按触发器类型 `n2` 调用对应分支切 `w`：
  - 类型 1（战斗波）→ `a(5)`（j.java:1053）
  - 类型 2（剧情/Boss 脚本，先读 `i[0]/i[1]/i[2]`）→ `a(6)`（j.java:1079）
- 任务对话 `w=4` 由 `e()`（剧情脚本，`j.java:253`）内多处剧情节点触发 `a(4)`（如 j.java:310/368/409/425/449/482/503/617/629/646），并不由触发器直接进入。
- `b==22`（任务简报）期间只渲染场景（`z.a(graphics)`/`z.b(graphics)`），**不调用无参 `z.b()`**（无参 `z.b()` 整个 a.java 仅 case 10 的 a.java:373 调用一次），故场景子状态机 `j.w` 被冻结、不步进；黑幕收起后由 `a.java:342-344` 将 `b` 切回 10、恢复 case 10 的 `z.b()` 更新。

---

## 4.1 `w==4` 任务对话推进（`j.f()`，j.java:672-752）

> 据 CFR 恢复。此前 jadx 反编译失败，`w==4` 的推进/退出条件未知。
> 本节行号基于权威 CFR 源 `reverse/game2/2-decompiled-cfr/tjge/`。
> `j.b()` 在 `w==4` 时每帧调 `this.f()`（j.java:172-174）；同帧 `j.a(graphics)` 在 `w==4` 时调 `c(graphics)`（j.java:247-249）绘制底部对话框。两者共享静态游标 `a.F[]` 与对话数组 `j.i[5]`/`j.j[3]`（定义 `j.java:34-35`，初始化 `j.java:1357-1358`）。

**渲染侧 `c(graphics)`（j.java:754-771）** 每帧：画底部对话框框体与说话人头像（`j[1]` 选头像与坐标），然后计算两个布尔写回 `f()` 使用：

- `this.H = L.c(a.F[0])`：当前段（第 `F[0]` 行对话）是否为**最后一段**（`a.c(int)`，a.java:943）。
- `this.I = L.a(graphics, a.F[1], a.F[2], nArray2[j[1]], 178, 90, 19)`：本段是否**已整段显示完**（`a.a(...)`，a.java:959）。
- 末尾 `L.c=0` 清按键。

**推进侧 `f()`（j.java:672-752）** 时序：

1. 取键判断（j.java:675-679）：若**确认键 `c==16`**（`bl`）则强制推进；否则 `a.F[3]--`，仍 `>=0` 时本帧停顿、`block22` 直接跳过推进。
2. 触发推进后按三类情况分支：
   - **`H`（最后一段）**（j.java:681-705）：本段对话讲完，按当前剧情类型 `i[0]` 收尾——
     - `i[0]==1` → 设 `i[1]=2`、`a(1)` 回剧情演出；
     - `i[0]==3` → `L.a()`（进 `b=22` 任务简报，`a.java:576`）；
     - `i[0]==4` → 设 `i[1]=1`、`a(1)`；
     - `i[0]==5` → 设 `i[1]=3`、`a(1)`；
     - `i[0]==6` → `L.a(true)`（本段为最后段时触发本关胜利结算，`L` 类型为 `a`/GameCanvas，故调 `a.a(boolean true)` → `b=16` LEVEL_CLEAR，见 a.java:552-565；注意勿与 j.java:113 的 `j.a(boolean)` 相机跟随混淆）。
   - **`I`（本段已满但非末段）**（j.java:706-744）：翻到下一段——`F[0]++`、`F[1]=0`、`F[3]=60`；并交换说话人 `j[1]↔j[2]`、推进对话行计数 `j[0]++`；随后按 `i[0]` 在到达特定行数时提前结束对话（如 `i[0]==1` 且 `j[0]==6`、`i[0]==3` 且 `j[0]==4`、`i[0]==5` 且 `j[0]==2` 时设 `i[1]=1`、`a(1)`；`i[0]==6` 且 `j[0]==2` 触发特殊处理 `L.a()`，`j[0]==6` 设 `i[1]=2`、`a(1)`）。
   - **本段未显示完**（j.java:745-748）：继续下卷 `F[1]+=F[2]`、`F[3]=60`。
3. 每帧末尾复位 `H=false; I=false`（j.java:750-751）。

> 要点：`w==4` 没有自身的"黑幕"出入场，它是**叠加在 `w` 返回态之上**的底部文字框；退出时通过 `a(1)`（回剧情）、`L.a()`（进任务简报 `b=22`）或 `L.a(true)`（本关胜利结算 → `b=16`，`L` 为 GameCanvas，见 a.java:552-565）三条路径之一离开，而非简单回到 `x`。
> ⚠️待核实：`i[0]` 的取值（1/3/4/5/6）对应的具体剧情语义、各 `j[0]` 行数阈值与具体关卡的对应关系，需结合 `string.bin` 对话文本与各关脚本进一步核对。

---

## 5. 状态转移图

```
                         ┌──────────────────────────────────────────────┐
                         │                                              │
   [构造 b=3]            ▼                                              │
        │          ┌─────────┐  j==65   ┌──────────────┐ j满23步        │
        └────────► │ 3 SPLASH│ ───────► │ 1 LOADING    │ ──────┐       │
                   └─────────┘          │   PROGRESS   │       │       │
                                        └──────────────┘       ▼       │
                                                          ┌──────────┐ │
            ┌─────────────────────────────────────────►  │ 4 MAIN   │ ◄┘
            │   (左软键/返回/失焦/各页返回)                │   MENU   │
            │                                             └────┬─────┘
            │            ┌──────────┬──────────┬───────────┬───┴────┬─────────┐
            │            │新游戏     │继续(有档) │继续(无档)  │帮助     │关于     │退出
            │            ▼          ▼          ▼            ▼        ▼        ▼
            │      ┌─────────┐ ┌────────┐ ┌──────────┐ ┌──────┐ ┌───────┐ destroyApp
            │      │20 CUT-  │ │ 2 LEVEL│ │100 NO    │ │6 HELP│ │19 ABOUT│
            │      │SCENE    │ │  LOAD  │ │  SAVE    │ └──┬───┘ └───┬───┘
            │      └────┬────┘ └───┬────┘ └────┬─────┘    │返回      │返回
            │           │ j满      │ M=true    │ j>15     └─►4◄──────┘
            │           └──►4      ▼           └──►4
            │                 ┌──────────┐
            │  失焦/键21       │ 10       │ ◄──────────────┐
            └──────────────── │ IN_GAME  │                │ 黑幕收起(e<0)
                              └──────┬───┘                │
            键22(z.w==0,e=0;b=22)    │                    │
                                     ▼                    │
                              ┌──────────────┐            │
                              │ 22 MISSION    │ ──────────┘
                              │   BRIEF       │ (键21→4 退菜单)
                              └──────────────┘
                  本关结束 GameCanvas.a(bl)  (CFR: a.a(boolean))
                                 │胜(bl=true) │败(bl=false)
                                 ▼            ▼
                           ┌──────────┐  ┌──────────┐
                           │16 LEVEL  │  │18 LEVEL  │
                           │  CLEAR   │  │  FAILED  │
                           └──┬────┬──┘  └──┬────┬──┘
                       非末关 │ 末关│  重玩  │    │返回
                       (k++)  │k==6 │        ▼    ▼
                         ▼    ▼     └──────► 2    4
                         2  ┌──────────┐
                            │21 GAME   │ j>30
                            │ COMPLETE │ ─────► 4
                            └──────────┘
```

---

## 6. 存疑清单（状态机相关）

1. ✅已据 CFR 解决：`case 20`（`a(Graphics)`，a.java:583）为新游戏开场剧情过场，子状态 `e` 驱动区域框渐入 + 逐字对话，出口 `b=2` 进首关加载（详见 §2.1）。原 jadx 推测"复用 case 1 进度、回 `b=4`"有误，已订正。
2. ✅已据 CFR 解决：`j.f()`（j.java:672）为 `w==4` 任务对话的逐字推进，退出经 `a(1)`/`L.a()`/`L.a(true)` 三条路径（详见 §4.1）。
3. `pauseApp()`（GameMIDlet.java:59-66）会把 `b==10` 切到 `b=4`，而 `hideNotify()`（a.java:567-574）也对 `b==10`/`b==22` 做 `b=4`——两者触发时机（来电/盖屏 vs 失焦）需实机区分。⚠️
4. `b` 缺号（0/5/7/8/9/11-15/17）确认未在 game2 使用，是引擎历史遗留；若复刻可直接忽略。
5. ⚠️待核实：§2.1/§4.1 中遗留的剧情数据语义——开场对话双说话人位置（`a.A/a.B`）、`w==4` 对话的 `i[0]` 类型与 `j[0]` 行数阈值，需结合 `string.bin` 文本与各关脚本进一步核对。
