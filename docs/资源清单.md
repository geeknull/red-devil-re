# 资源清单（两游戏全量解码结果）

> 由 `tools/extract_bin.py game1/game2 all` 生成。"有效条目"= count − 1（末项为 EOF 哨兵）。
> PNG 已解码到 `reverse/<game>/assets/images/`，其余转储到 `reverse/<game>/assets/rawdump/<bin>/`。

## 游戏1 红魔特种兵

| 文件 | 字节 | 有效条目 | 类型 | 内容 |
|---|---|---|---|---|
| `image.bin` | 18789 | 9 | PNG | UI/精灵图：142×43、62×28、139×74、187×41、144×112、128×112、128×96、48×50、116×60 |
| `a.bin` | 3209 | 23 | 自定义 | 精灵/动作帧数据（每条头部含索引+宽高+帧块，待逐字段还原） |
| `f.bin` | 13163 | 8 | 自定义 | 疑似瓦片集/地图帧（头部 `宽 高 10 101010...`） |
| `s.bin` | 3190 | 8 | 自定义 | 疑似精灵定义（头部 `01 09 14 00 8080...`） |
| `t.bin` | 39329 | 16 | 自定义 | 最大，疑似关卡/地图数据（头部 `4c 00.. 0d11..`） |
| `x.bin` | 1088 | 3 | UTF-16LE 字符串 | 帮助/关于等文本（BOM `FF FE`） |
| `sound.bin` | 5352 | 6 | 音频 | entry3-5=WAV(RIFF/WAVE)；entry0-2=自定义短音（头 `02 4a 3a 40`） |

PNG 尺寸明细（来自 `file`）：
```
image_00 142×43(4bit)  image_01 62×28   image_02 139×74  image_03 187×41
image_04 144×112(8bit) image_05 128×112 image_06 128×96  image_07 48×50  image_08 116×60
```

## 游戏2 红魔特种兵2-深海战舰

| 文件 | 字节 | 有效条目 | 类型 | 内容 |
|---|---|---|---|---|
| `actorPng.bin` | 15781 | 9 | PNG | 角色/敌人精灵图 |
| `logo.bin` | 10594 | 6 | PNG | Logo/标题/过场图 |
| `fpng.bin` | 7057 | 2 | PNG | 疑似字体或界面图 |
| `bpng.bin` | 467 | 1 | PNG | 疑似背景图块 |
| `a.bin` | 6444 | 23 | 自定义 | 动作/帧数据（每条以 `0x11` 开头） |
| `b.bin` | 848 | 2 | 自定义 | 瓦片数据（头 `0000 0800 5000 0808`） |
| `m.bin` | 4128 | 7 | 自定义 | **地图**（头 `01 00 0404 W H ffffffff`，tile 4×4，W/H 为关卡尺寸） |
| `p.bin` | 5244 | 2 | 自定义 | 疑似图块/调色板（头 `0000 1000 .. 0808`） |
| `s.bin` | 5301 | 7 | 自定义 | 精灵定义（头 `b0 00 b0 00 ...`，0xb0=176） |
| `t.bin` | 1593 | 9 | 自定义 | 关卡/触发数据（每条以 `0x10` 开头） |
| `string.bin` | 3058 | 9 | UTF-16 中文 | 游戏文本（编码细节见游戏2 资源映射） |
| `sound.bin` | 477 | 2 | **标准 MIDI** | 背景音乐（`MThd` 头） |
| `vssver.scc` | — | — | — | VSS 版本控制残留文件，非游戏资源，忽略 |

## 备注

- 自定义二进制（`a/f/s/t/b/m/p`）的**逐字段结构**需结合各模块 loader 还原，见各游戏 `资源映射.md`。
- 游戏2 `actorPng`/`fpng`/`bpng` 命名暗示"图块按用途分包"，与游戏1 单一 `image.bin` 不同——这是两代引擎的演进点。
