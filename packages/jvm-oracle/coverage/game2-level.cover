# game2-level 必达路径断言
#
# 为什么需要这个文件：
#   差分「绿」只在**真的走到目标路径**时才有信息量。绿而没走到 = 没意义（见 docs/复盘-CFR不是权威.md）。
#   本清单把「这条 scenario 覆盖了哪些路径」从**偶然**变成**机器断言的不变量**：
#   若将来有人改动 scenario / 输入脚本，导致这些路径不再被走到，门禁**红**而不是悄悄变成一个空洞的绿灯。
#
# 格式（# 开头为注释）：
#   op    <最少出现次数> <op 子串>   | <说明>
#   state <状态号>                   | <说明>
#
# 状态号取自 packages/game2/src/constants.ts 的 UiState 枚举。

# ── actor 换色帧（PLTE 补丁帧）──────────────────────────────────────────────
# 这是移植方**自创**的机制（原版在运行时给 PNG 打 PLTE 补丁再解码；兼容层无运行时同步 PNG 解码，
# 改为 TileSheet.a_PaletteFrames() 离线重算补丁字节 + 预解码 + 按帧登记）。
# 「原版没得抄、移植方不得不自己发明」正是两次真缺陷的高发区，故必须锁死其覆盖。
#
# 为什么它恰好可在 op 层验：两侧图像身份都是**源 PNG 字节哈希**，且两侧的补丁字节是
# **各自独立算出来的**（oracle = 原版字节码自己打补丁；port = a_PaletteFrames 重算）
# → port 只要把补丁字节算错一位，drawRegion 的 png:<hash> 立刻不同 → 红。**不是自己验自己。**
#
# 实测：t.bin 条目 0（玩家+全部步兵敌人共用的人形图集）是全游戏**唯一**有换色帧的图集
# （imageCount=2）；换色帧 = 敌兵第二套制服配色，由关卡放置数据 palette(byte[6])=1 挑中。
op 1 png:5498ccc2bbb4 | actor 换色帧 frame1（t.bin条目0 的 PLTE 补丁帧）——自创机制的唯一 op 层验证点
op 1 png:28fafafc267c | actor 基础帧 frame0（同图集未打补丁的原字节）——与换色帧成对出现，两者身份不同才证明补丁真的生效

# ── 状态覆盖 ──────────────────────────────────────────────────────────────
state 10 | InGame（关卡内主玩法；换色帧自 F134 起在此状态下绘制）
state 22 | MissionBrief（任务简报）
state 20 | CutsceneIntro（新游戏开场过场）
state 4  | MainMenu（主菜单）
