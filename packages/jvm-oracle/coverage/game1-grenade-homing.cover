# game1-grenade-homing 必达路径断言
#
# 本夹具的存在意义 = 锚住**玩家手雷制导弹道**这一条路径（由 fuzz seed=110 捞出、
# 见 docs/复盘-CFR不是权威.md）。若它哪天不再走到手雷，绿灯就退化成一个什么都没验的空壳，
# 而**现有 behavior-net golden 覆盖不到此路径**（既有场景只开武器0=导弹，不走
# computeHomingTrajectory）→ 一旦此夹具空转，该缺陷的防线就**彻底没了**且无人察觉。
# 故这里显式断言它确实打出了手雷、且弹道就是原版那条。
#
# 格式：op <最少次数> <op 子串（可含空格）> | <说明>
#       state <状态号> | <说明>

# ── 手雷弹道（本夹具的靶心）──────────────────────────────────────────────
# 25x9 = 手雷精灵尺寸；d=74,83 = 原版（真字节码）在 F187 的位置。
# 历史缺陷（CFR 反编译错、port 照抄）会把它变成 d=74,56 —— 每帧窜高 29px、三帧飞出屏幕。
# 真字节码裁决：`n4 /= n` 是**无条件**的，除法在三元分支**之前**（javap tjge.l.f() 214-237）。
# 算术印证：29696/26 = 1142 → 真机 -1142（≈-1px/帧，近乎平飞）。
op 1 blitSprite 25x9 t=0 d=74,83 | F187 手雷精灵的原版位置——历史缺陷会把它变成 d=74,56（该 op 消失即回归）

# ── 状态覆盖 ──────────────────────────────────────────────────────────────
state 10 | Playing（手雷只可能在关卡内主玩法中被投出）
