# game2-fail-level2 必达路径断言
#
# 本夹具的存在意义 = 锚住 **LevelFailed(18) 结算屏**这条从未与原版对拍过的路径：
# Continue 跳关卡2 → 过开场简报 → 走右撞敌交叉火力致死 → showResult(false) → LevelFailed(18)。
# 这是**结算渲染**（与 LevelClear 共用 GameCanvas.showResult→elapsedSeconds 时钟驱动计时），
# 此前任何 scenario/存档注入/模糊器都到不了（模糊器 150+33 轮杀不死玩家；game2-level 从不死）。
# 若哪天有人改动脚本致其不再走到 LevelFailed 屏，这里红，而不是悄悄退化成空洞绿灯。
#
# 断言取 **oracle 侧**（信任根＝原版字节码）：断言的是「原版在这条输入下确实走了该路径」。
# 格式：op <最少次数> <op 子串（可含空格）> | <说明>
#       state <状态号> | <说明>

# ── 状态覆盖 ────────────────────────────────────────────────────────────────
state 10 | Playing（关卡内主玩法 + 走右撞敌都发生在此）
state 22 | MissionBrief（关卡2 开场简报——走 enterBriefing 而非 MissionDialog，避开字体度量盲区）
state 18 | LevelFailed（玩家阵亡结算屏；此前从未被对拍过）

# ── LevelFailed 结算屏文字（本夹具的靶心：结算渲染逐字节忠实）──────────────────
# 这些 drawStr 只在 LevelFailed(18) 出现，携带任务编号/击杀数/继续次数/耗时/选项，
# 是「确实走到了失败结算屏」且「port 与原版逐字节一致」的机器证据。
op 20 drawStr [任务三失败] 88,30,17   | 失败标题「任务三失败」（关卡2=第三关）——只有到达 LevelFailed 才画
op 20 drawStr [击毙敌人:] 36,64,20    | 结算·击毙敌人数标签
op 20 drawStr [继续次数:] 36,90,20    | 结算·继续次数标签
op 20 drawStr [所用时间:] 36,116,20   | 结算·耗时标签
op 20 drawStr [00:20] 140,116,24      | 结算·耗时值（时钟已虚拟化 patch-clock，两侧逐字节一致——覆盖了钟表驱动的结算渲染，正是本单元修复的时钟路径）
op 20 drawStr [继续] 120,150,17       | 失败屏「继续」选项
op 20 drawStr [菜单] 120,170,17       | 失败屏「菜单」选项
